{
  "name": "dotvault-frontend",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.mjs",
    "watch": "node esbuild.config.mjs --watch"
  },
  "dependencies": {
    "preact": "^10.29.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
